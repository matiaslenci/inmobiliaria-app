<!DOCTYPE html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Resultado de Consulta - Tasas Inmobiliarias</title>

    <!-- Bootstrap 5 CDN -->
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />

    <style>
      body {
        background: #f5f7fa;
        min-height: 100vh;
        padding-top: 40px;
        padding-bottom: 40px;
      }
      .container {
        max-width: 900px;
        background: white;
        padding: 2rem;
        border-radius: 1rem;
        box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
      }
      h1 {
        margin-bottom: 1.5rem;
        text-align: center;
      }
      table th,
      table td {
        vertical-align: middle;
        text-align: center;
      }
    </style>
  </head>

  <body>
    <div class="container">
      <h1>Resultado de Consulta</h1>

      <table class="table table-bordered table-hover">
        <thead class="table-primary">
          <tr>
            <th>Cuenta Agua</th>
            <th>Monto Agua</th>
            <th>Cuenta Tasa</th>
            <th>Monto Tasas</th>
          </tr>
        </thead>
        <tbody>
          <% for(let i = 0; i < Math.max(cuentasAgua.length,
          cuentasTasas.length); i++) { %>
          <tr>
            <td><%= cuentasAgua[i] || "-" %></td>
            <td><%= montosAgua[i] || "-" %></td>
            <td><%= cuentasTasas[i] || "-" %></td>
            <td><%= montosTasas[i] || "-" %></td>
          </tr>
          <% } %>
        </tbody>
      </table>

      <div class="text-center mt-4">
        <a href="/" class="btn btn-primary">Volver</a>
      </div>
    </div>

    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"
      crossorigin="anonymous"
    ></script>
  </body>
</html>
