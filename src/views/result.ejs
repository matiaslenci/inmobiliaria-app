<!DOCTYPE html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Resultados - Cuentas Agua</title>
    <!-- Bootstrap 5 -->
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <style>
      body {
        background: #f5f7fa;
        padding: 2rem;
      }
      .card {
        border-radius: 1rem;
        box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
      }
    </style>
  </head>
  <body>
    <div class="container">
      <div class="card p-4">
        <h3 class="mb-4">💧 Lista de Cuentas de Agua</h3>
        <table class="table table-bordered table-hover">
          <thead class="table-primary">
            <tr>
              <th>#</th>
              <th>Cuenta Agua</th>
            </tr>
          </thead>
          <tbody>
            <% cuentas.forEach((cuenta, index) => { %>
            <tr>
              <td><%= index + 1 %></td>
              <td><%= cuenta %></td>
            </tr>
            <% }) %>
          </tbody>
        </table>
        <a href="/" class="btn btn-primary mt-3">⬅ Volver</a>
      </div>
    </div>
  </body>
</html>
